/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
        './src/app/admin/**/*.{js,ts,jsx,tsx}',
        // If you use any shared components inside /admin, add them too
    ],
    theme: {
        extend: {},
    },
    plugins: [],
};